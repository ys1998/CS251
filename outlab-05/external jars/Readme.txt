add all jar files to $CLASSPATH using "export CLASSPATH=$CLASSPATH:/home/usr_name/add_to_files" (address seprators are : and if you want to reset CLASSPATH, just wrtie export CLASSPATH= ) NOTE: the system does not tell you if you have inserted the wrong file in the classpath so be sure before adding it
all citations have been put inside Problem1.java 
