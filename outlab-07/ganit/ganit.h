#ifndef GANIT_GANIT_H
#define GANIT_GANIT_H

#include <math.h>
/* Add any other includes you need here */

/* Uncomment each declaration as you implement them. */

double factorial (double a);
double isPrime (double a);
double _floor (double a);
double _abs (double a);
double _ceil (double a);

/*
  Remember that each of the files in ganit must compile into separate object files.
  The object files must be archived into a static library whose name is libganit.a
  Modify the Makefile rule for calc so that it can link against libganit.a
*/


#endif /* !GANIT_GANIT_H */
